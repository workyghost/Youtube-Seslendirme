{
  "name": "youtube-translator-narrator",
  "private": true,
  "version": "3.0.0",
  "scripts": {
    "test": "jest --testPathPattern=tests/"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.cjs"
    ]
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.3.0"
  }
}
